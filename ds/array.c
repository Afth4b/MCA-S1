#include<stdio.h>

int main()
{
    int n,a[];
    printf("Enter the number of elements of array");
    scanf("%d",&n);
    printf("Enter the elements of array");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
}